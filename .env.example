# API
NODE_ENV=development
PORT=3001

# Add local service credentials here when integrations are introduced.
# Never commit real credentials. Keep this file limited to variable names and safe defaults.
